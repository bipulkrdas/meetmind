# Gemini CLI Configuration

## LiveKit MCP Server

For any questions or prompts related to LiveKit, the Gemini CLI will use the configured MCP server 'livekit-docs'. This is not a general search provider, but is specifically for queries related to LiveKit.
